[package]
name = "api"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "server"

[[bin]]
name = "migrate"

[package.metadata.scripts]
dev = "echo Recompiling on changes..."
db:up = "echo Starting PostgreSQL..."
db:down = "echo Stopping database..."

[dependencies]
