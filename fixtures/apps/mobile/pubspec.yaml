name: mobile_app
description: Mobile application
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

scripts:
  build:android: echo "Generating Android APK..."
  build:ios: echo "Building iOS archive..."
  test: echo "Running widget tests..."
  analyze: echo "Checking Dart code..."
  gen: echo "Generating code from annotations..."
